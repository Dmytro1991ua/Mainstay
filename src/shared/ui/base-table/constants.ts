/** id of the scroll container that InfiniteScroll watches (must be unique per mounted table). */
export const SCROLL_WRAPPER_ID = "base-table-scroll";

export const DEFAULT_EMPTY_MESSAGE = "No records found.";
export const DEFAULT_EMPTY_SEARCH_MESSAGE = "No records match your search.";
export const DEFAULT_ERROR_MESSAGE = "Something went wrong while loading data.";

/** Debounce for the search box before it hits the server (ms). */
export const SEARCH_DEBOUNCE_MS = 350;
