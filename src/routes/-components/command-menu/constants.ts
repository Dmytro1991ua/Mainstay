/** Key that opens the command palette (with ⌘ on macOS / Ctrl elsewhere). */
export const COMMAND_MENU_KEY = "k";

/** Group headings shown in the command palette. */
export const COMMAND_GROUP = {
  NAVIGATION: "Navigation",
  ACTIONS: "Actions",
} as const;
